{
  "buildCommand": "python3 -m ensurepip && pip3 install --upgrade pip && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o yt-dlp && chmod +x yt-dlp && npm run build",
  "installCommand": "python3 -m pip install -r requirements.txt && npm install",
  "framework": "nextjs",
  "regions": ["fra1"]
}
